import React from 'react';
import Input from './Input';

const SearchBar = () => {
  return <Input width='450px' height='50px' $placeHoder='검색할 단어를 입력해주세요.' autoFocus />;
};

export default SearchBar;
